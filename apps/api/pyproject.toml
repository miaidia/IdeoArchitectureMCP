# plot-api — workspace member (app entry point). Stub; implementation deferred to a later phase.
[project]
name = "plot-api"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["plot-shared", "plot-domain"]

[tool.uv.sources]
plot-shared = { workspace = true }
plot-domain = { workspace = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["plot_api"]
