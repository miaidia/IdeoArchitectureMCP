# plot-connectors — workspace member. Stub package (no runtime code yet); deferred to a later phase.
[project]
name = "plot-connectors"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["plot_connectors"]
